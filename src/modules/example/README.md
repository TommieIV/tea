# Example Module

This is a deliberately non-business demonstrative module for TEA v0.1.

It is registered in the Core module registry and is lazy loaded at `/modules/example`. Its permission is `example.overview.view`, and the supplied seed enables it only for the seeded Personal workspace.

Do not add real product functionality here. The first real module should receive its own focused specification and module directory.
